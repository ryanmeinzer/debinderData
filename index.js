const data = require('./data.json')

function transformData(data) {

    console.log(Object.keys(data))

    // let obj = {}
    // for (let el in data) {

    // }


    // console.log(data)
}

transformData(data)